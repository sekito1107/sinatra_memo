<div class="memo-list">
  <ul>
    <% @memos.each do |memo| %>
      <li>
        <a href="/memos/<%= memo["id"] %>"><%= h(memo["title"]) %></a>
      </li>
    <% end %>
  </ul>
</div>
<div>
  <button class="button" onclick="location.href='/memos/new'">作成</button>
</div>
